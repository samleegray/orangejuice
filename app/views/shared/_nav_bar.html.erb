<nav class="bg-gray-800 text-white">
  <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
    <div class="flex justify-between h-16">
      <div class="flex items-center space-x-4">
        <%= link_to root_path do %>
          <h1 class="font-bold text-2xl">Orangejuice</h1>
        <% end %>
        <% if user_signed_in? and current_user.is_admin? %>
          <%= link_to 'Posts', posts_path, class: "nav-button" %>
          <%= link_to 'Roles', roles_path, class: "nav-button" %>
        <% end %>
      </div>
      <% if user_signed_in? %>
          <div class="flex items self-center">
            <%= button_to "Sign Out", destroy_user_session_path, method: :delete, class: "bad-button" %>
          </div>
        <% else %>
          <div class="flex items space-x-5">
            <%= link_to "Sign In", new_user_session_path, class: "good-button" %>
            <%= link_to "Sign Up", new_user_registration_path, class: "good-button" %>
          </div>
        <% end %>
      <!-- Other navigation links or elements -->
    </div>
  </div>
</nav>